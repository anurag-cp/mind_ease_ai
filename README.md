app.py is the main code 
install the necessary python libraries (better create a environment inside the folder and install the libraries inside the environment)
emo_detect.csv - is the emotion data set used for training the ML
emo_detect.ipyng - it is the ML modal trained in Jupyter notebook 
text_emo.pkl is the result of trained modal used for emotion detection
